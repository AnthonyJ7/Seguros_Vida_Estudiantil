## Backend environment example for Firebase Admin
# Copy to .env and fill the values, then restart `npm run dev`

# Required project id
FIREBASE_PROJECT_ID=segurosvidaestiudiantilutpl

# Option A: Inline JSON (paste entire service account JSON as one line)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# Option B: Path to service account file (absolute path on Windows)
# GOOGLE_APPLICATION_CREDENTIALS=C:\\path\\to\\serviceAccount.json

# Option C: Separate vars
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxx@segurosvidaestiudiantilutpl.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Optional
PORT=4000
# FIREBASE_STORAGE_BUCKET=segurosvidaestiudiantilutpl.appspot.com
